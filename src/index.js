import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import AddCoordinates from './Form';

ReactDOM.render(
  <React.StrictMode>
    <AddCoordinates />
  </React.StrictMode>,
  document.getElementById('root')
);
